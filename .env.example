# Database (Railway PostgreSQL or local SQLite)
DATABASE_URL="postgresql://user:password@host:5432/khashn"

# For local development with SQLite (default):
# DATABASE_URL="file:./dev.db"

# Node environment
NODE_ENV=production

# Port (Railway sets this automatically)
PORT=3001
