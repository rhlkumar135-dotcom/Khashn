[build]
builder = "nixpacks"
buildCommand = "bun install && bun run generate && bun run build"

[deploy]
startCommand = "bun run server.tsx"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[env]
NODE_ENV = "production"
