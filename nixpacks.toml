[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install --ignore-scripts", "if [ -n \"$DATABASE_URL\" ]; then sed -i 's/provider = \"sqlite\"/provider = \"postgresql\"/' prisma/schema.prisma; fi", "rm -rf src/generated/prisma && bun x prisma generate", "bun run generate"]

[phases.build]
cmds = ["bun run build"]

[start]
cmd = "bun run railway:setup && bun run start"
